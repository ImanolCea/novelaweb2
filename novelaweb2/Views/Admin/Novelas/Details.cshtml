@model novelaweb2.Models.Novela

@{
    ViewData["Title"] = "Detalle de novela";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <h2 class="text-light mb-3">📖 Detalle — @Model.Titulo</h2>

    <div class="card bg-dark text-light p-3 mb-3">
        <div class="row">
            <div class="col-md-3 text-center">
                <img src="@Model.PortadaUrl" class="img-fluid rounded mb-2" alt="@Model.Titulo" />
            </div>
            <div class="col-md-9">
                <dl class="row">
                    <dt class="col-sm-3">Título</dt>
                    <dd class="col-sm-9">@Model.Titulo</dd>

                    <dt class="col-sm-3">Autor</dt>
                    <dd class="col-sm-9">@Model.Autor?.NombreUsuario</dd>

                    <dt class="col-sm-3">Género</dt>
                    <dd class="col-sm-9">@Model.Genero</dd>

                    <dt class="col-sm-3">Estado</dt>
                    <dd class="col-sm-9">@Model.Estado</dd>

                    <dt class="col-sm-3">Sinopsis</dt>
                    <dd class="col-sm-9">@Model.Sinopsis</dd>
                </dl>
            </div>
        </div>
    </div>

    <a asp-action="Novelas" class="btn btn-secondary">
        <i class="fa fa-arrow-left"></i> Volver
    </a>
    <a asp-action="EditarNovela" asp-route-id="@Model.Id" class="btn btn-warning ms-2">
        <i class="fa fa-edit"></i> Editar
    </a>
</div>
