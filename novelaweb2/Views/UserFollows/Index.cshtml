@model IEnumerable<novelaweb2.Models.UserFollow>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaSeguimiento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdSeguidoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdSeguidorNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaSeguimiento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdSeguidoNavigation.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdSeguidorNavigation.Id)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
